/*
js is very dynamic language
you can use ' ; ' (semicolon ) or you can not use it still the will run*/
let a =1;
let  b = 0 // no required the semicolon
var d = 10;
console.log("the division is : ", a/b + " value of d is  ",d)

var d = "kunal" // this will not show and error
// if we redeclare var then js will not show an error (this is the feature and also it will give problems to the devl0opers so we use let to declare variable)
let c // undefined

console.log("the division is : ", a/b + " value of d is  ",d)